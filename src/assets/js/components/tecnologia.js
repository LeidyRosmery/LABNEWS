'use strict';
 const Tecnologia = () => {
   const tecnologia = $('<section class="principal">');
   const container= $('<div class="container-fluid ">');
   const row=$("<div class='row noticia'>");

    return tecnologia;

 }
